// import proxyquire from 'proxyquire';
// import { should } from "chai";
// should();

// describe('getAmounts(totalDailyAmount, bonePercentage, rmbPercent, otherPercentages)', () => {
//   let getAmounts;
  
//   before(() => {
//     getAmounts = proxyquire.noCallThru().load('./getAmounts', {
//       // './getMuscleAmount': (what) => (what),
//       // './getAmountByPercent': (what) => (what),
//       // './getBoneAmount': (what) => (what),
//     }).default;
//   });

//   it('should get the percentage of the amount - muscle', () => {
//     getAmounts(32, 65).should.equal(20.8);
//   });
// });
